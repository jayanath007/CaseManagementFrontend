export enum SideBarMenuMode {
    Compact = 'COMPACT',
    Full = 'FULL'
}

export enum RouterOutlets {
    Inbox = 'inbox',
    Drafts = 'drafts',
    MessagesCommon = 'common',
    MessageItemCommon = 'item',
    Groups = 'groups'
}
