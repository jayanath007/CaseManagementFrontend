export enum OpenFrom {
    MainView,
    OpenCase,
}

export enum TabIndex {
    Details,
    SearchResult,
    Document,
    Matterts
}

export enum Mode {
    Edit,
    New,
    BaseOnFile
}

export enum Section {
    Company = 'isCompanyDirty',
    Contact = 'isContactDirty',

}
