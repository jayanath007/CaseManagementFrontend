export enum TimeRecordedOption {
    VALUE = 'value',
    HOURS = 'hour',
    ALL  = 'all',
    CHARGEABLE  = 'chargeable'
}

export enum MatterTypeEnum {
    OPENED = 'opened',
    CLOESD = 'closed',
    GROWTH = 'growth'
}

export enum ChartType {
    BarChart = 'bar',
    LineChart = 'line'
}
